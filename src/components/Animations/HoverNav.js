
import React from 'react'
import { motion } from "framer-motion"

const NavHoverItem = () => {
    const HoverVariants = {
        initial: {
            opacity: 0
        },
        hover: {
            opacity: 1
          }
    }
}
    return {
        <motion.div></motion.div>
    }